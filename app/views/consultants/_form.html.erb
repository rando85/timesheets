<%= form_with(model: consultant) do |form| %>
  <% if consultant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consultant.errors.count, "error") %> prohibited this consultant from being saved:</h2>

      <ul>
        <% consultant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.collection_select :team_id, Team.all, :id, :team %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.text_field :is_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
