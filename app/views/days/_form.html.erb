<%= form_with(model: day) do |form| %>
  <% if day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(day.errors.count, "error") %> prohibited this day from being saved:</h2>

      <ul>
        <% day.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :week_id %>
    <%= form.text_field :week_id %>
  </div>

  <div class="field">
    <%= form.label :month_id %>
    <%= form.text_field :month_id %>
  </div>

  <div class="field">
    <%= form.label :date_str %>
    <%= form.text_field :date_str %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
